require 'rails_helper'
describe Domain do
  context "#create" do
    let(:awesome_domain) {FactoryGirl.build(:domain)}
     it "should create a new domain" do
        expect(subject).to  
     end
  end
end
